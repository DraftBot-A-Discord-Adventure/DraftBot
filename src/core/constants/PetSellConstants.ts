export abstract class PetSellConstants {
	static readonly SELL_PRICE_MIN = 100;

	static readonly SELL_PRICE_MAX = 50000;

	static readonly MIN_XP_DIVIDER = 20;

	static readonly MAX_XP_DIVIDER = 20 / 9;

	static readonly MAX_SPAM_COUNT_SELLER = 3;
}